/*
8. Write a program in C++ to find the last prime number occur before the entered number.
Sample Output:
Input a number to find the last prime number that occurs before the number: 50
47 is the last prime number before 50
*/
#include <iostream>
using namespace std;
#include <math.h>
int main()
{
    int  i,a;flag=0;
    cout<<"Enter the prime numbers : ";
    cin>>a>>endl;
    for(i=a;i>0;i--){
        if()
    }
    cout<<

    return (0);
}

